--!strict

export type Sword = {
    id: string,
    model: string,
    damage: number,
    swingTime: number
}

export type Bow = {
    id: string,
    model: string,
    damage: number,
    drawTime: number,
}

return {}
